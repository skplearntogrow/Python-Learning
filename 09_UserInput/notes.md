**Topics Covered:**
1. User Input is allowed in python by using input() function.
2. The input() function can have prompt as argument. Ex: input("Enter your name:")
3. By default any input user provides is a String. We can use float() function to convert string input to float
4. We can input Number by using float()
5. Validating Input we can use While loop, try except else finally. Based on user input we can provide print.
6. We can use Prompts directly inside input() function or we can assign it to a variable a= "Enter your name:" or a= input("Enter your name:")
7. Did some practice programs.